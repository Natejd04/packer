<div id="container">
    <div id="hikers">
    <h3><%= @hikers.name %></h3>
        <span>Here are the items you are responsible to bring</span>
            <ul>
                <% @hikers.items.each do |hiker| %>  
                    <li><%= hiker.name %></li>
                <% end %>
            </ul>
        </div>
</div>